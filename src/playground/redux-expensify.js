// import { createStore, combineReducers } from 'redux';

// const unsubscribe = store.subscribe(() => {
//     const state = store.getState();
//     const visibleExpenses = getVisibleExpenses(state.expenses, state.filters);
//     console.log(visibleExpenses);
// });

// store.dispatch(setStartDate(Date.now()));
// store.dispatch(setEndDate(Date.now() + (24 * 60 * 60 * 1000)));
// store.dispatch(setTextFilter());
// store.dispatch(sortByDate());

// const expenseOne = store.dispatch(addExpense({ description: 'coffee', amount: 500 }));

// setTimeout(() => {
//     store.dispatch(addExpense({ description: 'rent', amount: 100000 }));
// }, 2000);


// store.dispatch(removeExpense({ id: expenseOne.expense.id }));
// store.dispatch(editExpense(expenseTwo.expense.id, { amount: 500 }));
// store.dispatch(setTextFilter('rent'));
// store.dispatch(sortByAmount());
// store.dispatch(sortByDate());
// store.dispatch(setStartDate(Date.now()));
// store.dispatch(setStartDate());
// store.dispatch(setEndDate(Date.now()));




